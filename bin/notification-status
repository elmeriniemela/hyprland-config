#!/usr/bin/env bash
set -euo pipefail

if [[ $(dunstctl is-paused 2>/dev/null || printf false) == true ]]; then
    jq -cn '{text:"", tooltip:"Notifications paused", class:"paused"}'
else
    jq -cn '{text:"", tooltip:"Notifications enabled", class:"enabled"}'
fi
