#!/usr/bin/env bash
set -euo pipefail

hyprctl reload
sleep 0.5
errors=$(hyprctl configerrors)
if [[ -n $errors ]]; then
    notify-send -u critical "Hyprland configuration errors" "$errors"
    printf '%s\n' "$errors" >&2
    exit 1
fi
notify-send "Hyprland" "Configuration reloaded"
